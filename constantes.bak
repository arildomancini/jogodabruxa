#lang racket

;; Arquivo para definição das constantes do programa.
(require rackunit)
(require 2htdp/image)
(require 2htdp/universe)
(require racket/list)
;; Jean Carlos Neto Grr 2016 7741
;; Arildo Mancini Grr 2016 7512
;; Programa jogo mata a bruxa e seus bichos

;; ============================================================================================================================
;;CONSTANTES:

;; Constante do cenario:
(define LARGURA-CENARIO 1200)
(define ALTURA 700)
(define PAREDE (bitmap "paredes.png"))
(define CENARIO (rectangle LARGURA-CENARIO ALTURA "outline" "black"))
(define TELA-JOGO (place-image (beside PAREDE PAREDE) 0 ALTURA (rectangle LARGURA-CENARIO ALTURA "outline" "black")))

;;****************************************************************************************************************************
;; Constante do tiro

(define IMG-TIRO (ellipse 10 5 "solid" "black"))
(define Y (/ ALTURA 2))
(define MEIO-H-TIRO (/ (image-width IMG-TIRO) 2 ))
(define D-TIRO 30)

;;******************************************************************************************************************************
;; Constante da bruxa:

(define IMG-BRUXA-INO (flip-horizontal(scale 0.1 (bitmap "bruxa.png"))))
(define IMG-BRUXA-VORTANO (flip-horizontal IMG-BRUXA-INO))
(define X-B (/ LARGURA-CENARIO 2))
(define DY-B-DEFAULT 5)

;********************************************************************************************************************************  
;; Constante do morcego:

 (define IMG-ALVO-MOR-INO (scale 0.3(bitmap "morcego.png")))
 (define IMG-ALVO-MOR-VORTANO (flip-horizontal IMG-ALVO-MOR-INO))
 (define Y2 (/ ALTURA 2))
 (define Y-PADRAO 200)
 (define MEIO-H-MOR (/ (image-width IMG-ALVO-MOR-INO) 2 ))
 (define LIMITE-ESQ-MOR (- LARGURA-CENARIO MEIO-H-MOR))
 (define LIMITE-DIR-MOR (- LARGURA-CENARIO MEIO-H-MOR))

;***********************************************************************************************************************************
;;; Constante do corvo:

 (define IMG-ALVO-COR-INO (scale 0.2 (bitmap "corvo.png")))
 (define IMG-ALVO-CORVO-VORTANO (flip-horizontal IMG-ALVO-COR-INO))
 (define Y3 (/ ALTURA 2))
 (define MEIO-H-ALVO-COR (/ (image-width IMG-ALVO-COR-INO) 2 ))
 (define LIMITE-ESQ-COR (- LARGURA-CENARIO MEIO-H-ALVO-COR))
 (define LIMITE-DIR-COR (- LARGURA-CENARIO MEIO-H-ALVO-COR)) 

;;*****************************************************************************************************************************
;; constante da arma
(define IMG-ARMA-INO (scale 0.2 (bitmap "arma1.png"))) 
(define IMG-ARMA-VORTANO (flip-horizontal IMG-ARMA-INO))
(define MEIO-H-ARMA (/ (image-width IMG-ARMA-INO) 2 ))
(define MEIO-A-ARMA (/ (image-height IMG-ARMA-INO) 2 ))
(define MEIO-H-B (/ (image-width IMG-BRUXA-INO) 2 ))
(define LIMITE-DIREITO (- LARGURA-CENARIO MEIO-H-ARMA)) 
(define LIMITE-ESQUERDO MEIO-H-ARMA)
(define LIMITE-BAIXO (- ALTURA MEIO-A-ARMA)) 
(define LIMITE-CIMA MEIO-A-ARMA)
(define D-ARMA-DEFAULT 10)
;;*****************************************************************************************************************************
(define X-SPAWN LIMITE-DIREITO)
(define Y-SPAWN (* ALTURA 0.25))  
(define T-SPAWN (* 20 2)) 

;;================================================================================================================================